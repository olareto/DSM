@model DSM5.Models.Carrito

@{
    ViewBag.Title = "Details";
}
<div class="container-fluid bg-2">
    <h2 class="text-center bg-2">Carrito de compra</h2>
    <p class="bg-2">
        @Html.Partial("_Lineapedido", (IEnumerable<DSM5.Models.Lineas_pedido>)ViewBag.coment)
        @*
            @Html.ActionLink("Edit", "Edit", new { id = Model.id }) |
            @Html.ActionLink("Back to List", "Details", "Usuario", new { id = @Session["correo"] }, null)
        *@
    </p>
    <p class="bg-2">
        <h3 class="text-center bg-2">
            Precio total: @Html.DisplayFor(model => model.Precio)
            <a class="btn btn-default" href='@Url.Action("compra", "Carrito", new { id = Model.id }, null)'>Comprar</a>

        </h3>

    </p>
</div>
















    @*
        <div>
            <h4>Carrito</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Precio)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Precio)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Usuario)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Usuario)
                </dd>

            </dl>
        </div>
    *@


